import { _decorator, Component, Node, director } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('PKP_Canvas')
export class PKP_Canvas extends Component {
    // 设置为常驻节点
    // onLoad() {
    //     director.addPersistRootNode(this.node);
    // }
}


